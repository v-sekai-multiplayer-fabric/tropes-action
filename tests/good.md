# Clean sample

This document avoids the flagged patterns. It states facts directly and uses
ordinary punctuation. Lists describe each item as a sentence.

1. The launcher sets the environment and runs the program by absolute path.
2. The installer registers the service and starts it.

The cache stores compiled objects so repeat builds are faster.
