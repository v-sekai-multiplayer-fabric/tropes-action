# Training + export (CI only). AutoGluon MultiModal provides FT-Transformer and
# its NATIVE ONNX export (MultiModalPredictor.export_onnx) — no skl2onnx.
autogluon.multimodal
pandas
pyarrow
numpy
onnx
onnxruntime
